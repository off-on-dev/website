// Catch-all route (*) — renders the same 404 UI.
// RR7 requires each route to point to a unique file, so this wraps NotFound.
export { meta } from "@/pages/NotFound";
export { default } from "@/pages/NotFound";
