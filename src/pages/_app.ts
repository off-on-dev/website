import type { App } from "vue";

// Vue appEntrypoint: runs once per island before mount. Register app-wide Vue
// plugins or global components here. Currently empty — islands self-contain all
// their dependencies.
export default (_app: App): void => {};
